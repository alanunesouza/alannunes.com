import styled from 'styled-components';

import * as V from '../../styles/variables';

export const Container = styled.div`
  max-width: ${V.MaxSize.md};
  margin: auto;
  padding-top: ${V.Size.sm};
`;
